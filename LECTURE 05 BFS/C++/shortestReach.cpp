#include <iostream>
#include <queue>
using namespace std;
const int MAX = 1000;

void printPath(int s, int f) {
    int b[MAX];
    int m = 0;
    if (f == s) {
        cout << s;
        return; 
    }
    if (path[f] == -1) {
        cout << "No Path" << endl;
    }
}
int main() {
    int Q, N, M;
    cin >> Q;
    for (int i = 1; i <= Q; i++) {
        cin >> N >> M;
        for (int )
    }
    return 0;
}