#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int n;
    cin >> n;
    while (n != -1)
    {
        int energy, start, end;
        cin >> energy >> start >> end;
        while ()
        
        cin >> n;
    }
    return 0;
}